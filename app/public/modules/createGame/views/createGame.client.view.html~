<section class="createGameSection" data-ng-controller="CreateGameController">

<h1>Create a new game</h1>

<form name="myForm" class="createGameForm" novalidate>
	<label>Party name :</label> <input type="text" ng-model="newGame.title" required>

	<div ng-controller="CreateGameController">
<label>Choose the day of your fight !</label>
    <div class="row">
        <div class="col-md-6">
            <p class="input-group">
              <input type="text" class="form-control" datepicker-popup="{{format}}" ng-model="newGame.startDay" is-open="opened" min-date="minDate" max-date="maxDate" datepicker-options="dateOptions" date-disabled="disabled(date, mode)" ng-required="true" close-text="Close"/>
              <span class="input-group-btn">
                <button type="button" class="btn btn-default" ng-click="open($event)"><i class="glyphicon glyphicon-calendar"></i></button>
              </span>
            </p>
        </div>
</div>

<span>{{newGame.startDay}}</span>
	<label>Starting time :</label> 
			<select ng-model="newGame.startTime" >
			<option ng-repeat="hour in hours">{{hour.num}}</option>
			</select><br />

	<label>Minimum number of players :</label> 
			<select ng-model="newGame.minPlayers">
				<option value="2"> 2 </option>
				<option ng-show="newGame.maxPlayers >= 3" value="3"> 3 </option>
				<option ng-show="newGame.maxPlayers >= 4" value="4"> 4 </option>
				<option ng-show="newGame.maxPlayers >= 5" value="5"> 5 </option>
				<option ng-show="newGame.maxPlayers >= 6" value="6"> 6 </option>
			</select><br />
	
	<label> Maximum number of players :</label> 
			<select ng-model="newGame.maxPlayers">
				<option ng-show="newGame.minPlayers <= 2" value="2"> 2 </option>
				<option ng-show="newGame.minPlayers <= 3" value="3"> 3 </option>
				<option ng-show="newGame.minPlayers <= 4" value="4"> 4 </option>
				<option ng-show="newGame.minPlayers <= 5" value="5"> 5 </option>
				<option value="6"> 6 </option>
			</select><br />
	
	<!-- TODO : post le game même si formulaire pas rempli -->
	<input type="submit" ng-click="createGame()" value="Host" />

</form>
</section>
